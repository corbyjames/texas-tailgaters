services:
  - type: web
    name: texas-tailgaters
    env: static
    # Force clean build - updated Sep 13 2025
    buildCommand: rm -rf node_modules dist && npm install && npm run build
    staticPublishPath: ./dist
    headers:
      - path: /*
        name: X-Frame-Options
        value: sameorigin
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: 20